import styled from "styled-components";

export const TotalScores = styled.span`
    font-size: ${({ theme }) => theme.fontSize.l};
    color: ${({ theme }) => theme.colors.green};
    font-weight: 900;
`;