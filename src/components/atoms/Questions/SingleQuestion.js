import styled from "styled-components";

const StyledQuestion = styled.div`

`;

const SingleQuestion = ({  }) => {
    return (
        <StyledQuestion>
            <div>
                {/* <input type={type} id={id} name={name} value={value} istrue={istrue} />
                <label htmlFor={id}>{answer1}</label> */}
            </div>
        </StyledQuestion>
    )
}

export default SingleQuestion;
